[flake8]
select = E9,F63,F7,F82
count = True
show-source = True
statistics = True

[tool:pytest]
# Exclude the fingpt directory as it contains research notebooks and submodules
# with heavy ML dependencies (torch, transformers, etc.) not installed in CI.
addopts = --ignore=fingpt --tb=short -q
